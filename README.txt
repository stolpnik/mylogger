MyLogger

log your own data

Models:(! is user's changeable)
Log
!	what_to_do:text
!	date:date(default now)
!	private:private or not
	pub_date:publish date
	user_id:relations

Setting
!	name:name(or screen name)
